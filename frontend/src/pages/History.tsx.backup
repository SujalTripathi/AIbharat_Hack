import React, { useState, useEffect } from 'react';
import { History as HistoryIcon, FileText, MessageSquare, TrendingUp, Award, Clock, Target, CheckCircle, AlertCircle, Calendar, Download, Share2, Filter, Search, TrendingDown, BarChart } from 'lucide-react';
import { userAPI } from '../services/api';
import { getUserId } from '../utils/storage';
import LoadingSpinner from '../components/LoadingSpinner';
import Alert from '../components/Alert';
import Logo from '../components/Logo';

const History: React.FC = () => {
  const [history, setHistory] = useState<any>(null);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [activeTab, setActiveTab] = useState<'resume' | 'interview' | 'skillgap'>('resume');
  const [searchTerm, setSearchTerm] = useState('');
  const [sortBy, setSortBy] = useState<'recent' | 'score'>('recent');

  useEffect(() => {
    fetchHistory();
  }, []);

  const fetchHistory = async () => {
    const userId = getUserId();
    
    if (!userId) {
      setError('Please upload your resume first to view history');
      setLoading(false);
      return;
    }

    try {
      const response = await userAPI.getHistory(userId);
      setHistory(response.data.data);
    } catch (err: any) {
      setError(err.response?.data?.message || 'Failed to fetch history');
    } finally {
      setLoading(false);
    }
  };

  const formatDate = (date: string) => {
    return new Date(date).toLocaleDateString('en-US', {
      year: 'numeric',
      month: 'short',
      day: 'numeric',
      hour: '2-digit',
      minute: '2-digit'
    });
  };

  const getFilteredData = () => {
    let data: any[] = [];
    50 via-slate-900 to-slate-950 flex items-center justify-center">
        <div className="text-center">
          <div className="mb-8 flex justify-center animate-float">
            <Logo size="lg" showText={false} />
          </div>
          <LoadingSpinner message="Loading your history..." />
        </div>
      </div>
    );
  }

  if (error) {
    return (
      <div className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 flex items-center justify-center p-8">
        <div className="max-w-md">
          <Alert type="error" message={error} />
          <div className="text-center mt-6">
            <button
              onClick={() => window.location.href = '/resume'}
              className="px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-500 text-white rounded-xl font-semibold hover:scale-105 transition-all"
            >
              Upload Resume to Get Started
            </button>
          </div>
        </div>
      </div>
    );
  }

  const filteredData = getFilteredData();     const searchFields = [
          item.jobRole,
          item.jobId?.title,
          item.jobId?.company,
        ].filter(Boolean).join(' ').toLowerCase();
        return searchFields.includes(searchTerm.toLowerCase());
      });
    }

    // Sort data
    if (sortBy === 'recent') {
      data = data.sort((a: any, b: any) => 
        new Date(b.createdAt).getTime() - new Date(a.createdAt).getTime()
      );
    } else if (sortBy === 'score') {
      data = data.sort((a: any, b: any) => {
        const scoreA = a.atsScore || a.overallScore || a.matchPercentage || 0;
        const scoreB = b.atsScore || b.overallScore || b.matchPercentage || 0;
        return scoreB - scoreA;
      });
    }

    return data;
  };

  if (loading) {
    return (
      <div className="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 flex items-center justify-center">
        <LoadingSpinner message="Loading your history..." />
      </div>
    );
  }

  if (error) {
    return (
      <div className="min-h-screen bg-gradient-to-br from-slate-900 to-slate-800 flex items-center justify-center p-8">
        <Alert type="error" message={error} />
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-slate-950 py-8 sm:py-12 px-4 sm:px-8">
      {/* Background Pattern */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none opacity-30">
        <div className="absolute top-0 left-0 w-full h-full">
          <div className="absolute top-20 right-20 w-96 h-96 bg-indigo-500/20 rounded-full blur-3xl animate-pulse"></div>
          <div className="absolute bottom-20 left-20 w-96 h-96 bg-purple-500/20 rounded-full blur-3xl animate-pulse" style={{animationDelay: '1s'}}></div>
        </div>
      </div>

      <div className="max-w-7xl mx-auto animate-fadeIn relative z-10">
        {/* Enhanced Header */}
        <div className="text-center mb-8 sm:mb-12">
          <div className="inline-flex items-center justify-center w-20 h-20 sm:w-24 sm:h-24 bg-gradient-to-br from-indigo-500 via-purple-600 to-pink-600 rounded-3xl mb-6 animate-float shadow-2xl shadow-indigo-500/30">
            <HistoryIcon className="text-white" size={48} />
          </div>
          <h1 className="text-4xl sm:text-5xl md:text-6xl font-extrabold mb-4">
            <span className="bg-gradient-to-r from-indigo-400 via-purple-500 to-pink-600 bg-clip-text text-transparent">
              Your Career Journey
            </span>
          </h1>
          <p className="text-base sm:text-lg text-gray-300 max-w-2xl mx-auto leading-relaxed">
            Track your progress, review past analyses, and watch your skills grow over time
          </p>
        </div>

        {/* Enhanced Stats Overview */}
        {history?.stats && (
          <div className="grid grid-cols-1 sm:grid-cols-3 gap-4 sm:gap-6 mb-8 sm:mb-12">
            <div className="group backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-6 sm:p-8 text-center hover:bg-white/10 hover:scale-105 hover:border-blue-500/40 transition-all duration-300 shadow-xl">
              <div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-2xl mb-4 group-hover:scale-110 transition-transform">
                <FileText className="text-white" size={32} />
              </div>
              <div className="flex items-center justify-center gap-2 mb-2">
                <div className="text-4xl sm:text-5xl font-bold text-white">
                  {history.stats.latestAtsScore || 'N/A'}
                </div>
                {history.stats.atsScoreTrend && (
                  <TrendingUp className="text-green-400" size={24} />
                )}
              </div>
              <div className="text-sm text-gray-400 font-medium">Latest ATS Score</div>
              <div className="mt-2 text-xs text-gray-500">Resume Analysis</div>
            </div>
            
            <div className="group backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-6 sm:p-8 text-center hover:bg-white/10 hover:scale-105 hover:border-green-500/40 transition-all duration-300 shadow-xl">
              <div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-green-500 to-emerald-600 rounded-2xl mb-4 group-hover:scale-110 transition-transform">
                <MessageSquare className="text-white" size={32} />
              </div>
              <div className="text-4xl sm:text-5xl font-bold text-white mb-2">
                {history.stats.totalInterviews}
              </div>
              <div className="text-sm text-gray-400 font-medium">Mock Interviews</div>
              <div className="mt-2 text-xs text-gray-500">Practice Sessions</div>
            </div>
            
            <div className="group backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-6 sm:p-8 text-center hover:bg-white/10 hover:scale-105 hover:border-purple-500/40 transition-all duration-300 shadow-xl">
              <div className="inline-flex items-center justify-center w-16 h-16 bg-gradient-to-br from-purple-500 to-pink-600 rounded-2xl mb-4 group-hover:scale-110 transition-transform">
                <Award className="text-white" size={32} />
              </div>
              <div className="flex items-center justify-center gap-2 mb-2">
                <div className="text-4xl sm:text-5xl font-bold text-white">
                  {history.stats.averageInterviewScore}
                </div>
                <span className="text-yellow-400">★</span>
              </div>
              <div className="text-sm text-gray-400 font-medium">Avg Interview Score</div>
              <div className="mt-2 text-xs text-gray-500">Performance Rating</div>
            </div>
          </div>
        )}

        {/* Enhanced Search and Filter Bar */}
        <div className="backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-4 sm:p-6 mb-8">
          <div className="flex flex-col sm:flex-row gap-4">
            {/* Search */}
            <div className="flex-1 relative">
              <Search className="absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400" size={20} />
              <input
                type="text"
                placeholder="Search by job role, company..."
                value={searchTerm}
                onChange={(e) => setSearchTerm(e.target.value)}
                className="w-full pl-12 pr-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500/50 transition-all"
              />
            </div>

            {/* Sort Dropdown */}
            <div className="relative">
              <Filter className="absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400 pointer-events-none" size={20} />
              <select
                value={sortBy}
                onChange={(e) => setSortBy(e.target.value as 'recent' | 'score')}
                className="pl-12 pr-8 py-3 bg-white/5 border border-white/10 rounded-xl text-white focus:outline-none focus:ring-2 focus:ring-blue-500/50 focus:border-blue-500/50 transition-all appearance-none cursor-pointer min-w-[160px]"
              >
                <option value="recent" className="bg-slate-900">Most Recent</option>
                <option value="score" className="bg-slate-900">Highest Score</option>
              </select>
            </div>

            {/* Action Buttons */}
            <div className="flex gap-2">
              <button className="p-3 bg-white/5 hover:bg-white/10 border border-white/10 rounded-xl transition-all hover:scale-105 group">
                <Download className="text-gray-400 group-hover:text-white transition-colors" size={20} />
              </button>
              <button className="p-3 bg-white/5 hover:bg-white/10 border border-white/10 rounded-xl transition-all hover:scale-105 group">
                <Share2 className="text-gray-400 group-hover:text-white transition-colors" size={20} />
              </button>
            </div>
          </div>
        </div>

        {/* Enhanced Tabs */}
        <div className="backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-2 mb-8">
          <div className="grid grid-cols-3 gap-2">
            <button
              onClick={() => setActiveTab('resume')}
              className={`px-4 sm:px-6 py-3 sm:py-4 font-bold rounded-xl transition-all duration-300 flex items-center justify-center gap-2 text-sm sm:text-base ${
                activeTab === 'resume'
                  ? 'bg-gradient-to-r from-blue-600 to-cyan-600 text-white shadow-lg shadow-blue-500/30 scale-105'
                  : 'text-gray-400 hover:text-white hover:bg-white/5'
              }`}
            >
              <FileText size={20} className="hidden sm:block" />
              <span>Resume</span>
            </button>
            <button
              onClick={() => setActiveTab('interview')}
              className={`px-4 sm:px-6 py-3 sm:py-4 font-bold rounded-xl transition-all duration-300 flex items-center justify-center gap-2 text-sm sm:text-base ${
                activeTab === 'interview'
                  ? 'bg-gradient-to-r from-green-600 to-emerald-600 text-white shadow-lg shadow-green-500/30 scale-105'
                  : 'text-gray-400 hover:text-white hover:bg-white/5'
              }`}
            >
              <MessageSquare size={20} className="hidden sm:block" />
              <span>Interviews</span>
            </button>
            <button
              onClick={() => setActiveTab('skillgap')}
              className={`px-4 sm:px-6 py-3 sm:py-4 font-bold rounded-xl transition-all duration-300 flex items-center justify-center gap-2 text-sm sm:text-base ${
                activeTab === 'skillgap'
                  ? 'bg-gradient-to-r from-purple-600 to-pink-600 text-white shadow-lg shadow-purple-500/30 scale-105'
                  : 'text-gray-400 hover:text-white hover:bg-white/5'
              }`}
            >
              <TrendingUp size={20} className="hidden sm:block" />
              <span>Skills</span>
            </button>
          </div>
        </div>

        {/* Resume Analyses */}
        {activeTab === 'resume' && (
          <div className="space-y-6">
            {filteredData && filteredData.length > 0 ? (
              filteredData.map((analysis: any, index: number) => (
                <div 
                  key={analysis._id} 
                  className="group backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-6 sm:p-8 hover:bg-white/10 hover:scale-[1.02] hover:shadow-2xl hover:shadow-blue-500/20 transition-all duration-300 animate-slideUp" 
                  style={{ animationDelay: `${index * 0.1}s` }}
                >
                  <div className="flex flex-col sm:flex-row items-start justify-between gap-4 sm:gap-0 mb-6">
                    <div className="flex-1">
                      <div className="flex items-center gap-3 mb-3">
                        <div className="p-2.5 bg-gradient-to-br from-blue-500 to-cyan-600 rounded-xl group-hover:scale-110 transition-transform shadow-lg">
                          <FileText className="text-white" size={24} />
                        </div>
                        <div>
                          <h3 className="text-xl sm:text-2xl font-bold text-white group-hover:text-cyan-400 transition-colors">Resume Analysis #{history.resumeAnalyses.length - index}</h3>
                          <div className="flex items-center gap-3 text-sm text-gray-400 mt-1">
                            <div className="flex items-center gap-1.5">
                              <Calendar size={14} />
                              <span>{formatDate(analysis.createdAt)}</span>
                            </div>
                            <div className="flex items-center gap-1.5">
                              <BarChart size={14} />
                              <span>ATS Optimized</span>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div className="text-center sm:text-right">
                      <div className={`inline-flex items-center justify-center w-24 h-24 rounded-2xl border-4 shadow-xl ${
                        analysis.atsScore >= 80 ? 'text-green-400 bg-green-500/20 border-green-500/40 shadow-green-500/20' :
                        analysis.atsScore >= 60 ? 'text-yellow-400 bg-yellow-500/20 border-yellow-500/40 shadow-yellow-500/20' : 
                        'text-red-400 bg-red-500/20 border-red-500/40 shadow-red-500/20'
                      }`}>
                        <div>
                          <div className="text-3xl font-extrabold">{analysis.atsScore}</div>
                          <div className="text-[10px] font-medium opacity-75">SCORE</div>
                        </div>
                      </div>
                      <div className="text-xs text-gray-400 mt-2 font-medium">ATS Compatibility</div>
                    </div>
                  </div>

                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6">
                    <div className="bg-gradient-to-br from-green-500/10 to-emerald-500/10 border border-green-500/30 rounded-xl p-5 hover:border-green-500/50 transition-all">
                      <h4 className="text-sm font-bold text-green-400 mb-3 flex items-center gap-2">
                        <CheckCircle size={18} />
                        Key Strengths
                      </h4>
                      <ul className="text-sm text-green-100 space-y-2.5">
                        {analysis.analysis?.content?.strengths?.slice(0, 3).map((s: string, i: number) => (
                          <li key={i} className="flex items-start gap-2 leading-relaxed">
                            <span className="text-green-400 mt-0.5 font-bold">•</span>
                            <span>{s}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                    
                    <div className="bg-gradient-to-br from-red-500/10 to-orange-500/10 border border-red-500/30 rounded-xl p-5 hover:border-red-500/50 transition-all">
                      <h4 className="text-sm font-bold text-red-400 mb-3 flex items-center gap-2">
                        <AlertCircle size={18} />
                        Areas to Improve
                      </h4>
                      <ul className="text-sm text-red-100 space-y-2.5">
                        {analysis.analysis?.content?.weaknesses?.slice(0, 3).map((w: string, i: number) => (
                          <li key={i} className="flex items-start gap-2 leading-relaxed">
                            <span className="text-red-400 mt-0.5 font-bold">•</span>
                            <span>{w}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>
              ))
            ) : (
              <div className="backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-12 sm:p-16 text-center">
                <FileText className="text-gray-500 mx-auto mb-6 opacity-50" size={80} />
                <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">No Resume Analyses Yet</h3>
                <p className="text-gray-400 text-base sm:text-lg mb-6 max-w-md mx-auto">
                  Upload your resume to get instant ATS compatibility scores and AI-powered feedback
                </p>
                <button
                  onClick={() => window.location.href = '/resume'}
                  className="px-8 py-4 bg-gradient-to-r from-blue-500 to-cyan-500 text-white rounded-xl font-bold hover:scale-105 transition-all shadow-lg hover:shadow-blue-500/50"
                >
                  Upload Resume Now
                </button>
              </div>
            )}
          </div>
        )}

        {/* Interview Sessions */}
        {activeTab === 'interview' && (
          <div className="space-y-6">
            {filteredData && filteredData.length > 0 ? (
              filteredData.map((interview: any, index: number) => (
                <div 
                  key={interview._id} 
                  className="group backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-6 sm:p-8 hover:bg-white/10 hover:scale-[1.02] hover:shadow-2xl hover:shadow-green-500/20 transition-all duration-300 animate-slideUp"
                  style={{ animationDelay: `${index * 0.1}s` }}
                >
                  <div className="flex flex-col sm:flex-row items-start justify-between gap-4 sm:gap-0 mb-6">
                    <div className="flex-1">
                      <div className="flex items-center gap-3 mb-3">
                        <div className="p-2.5 bg-gradient-to-br from-green-500 to-emerald-600 rounded-xl group-hover:scale-110 transition-transform shadow-lg">
                          <MessageSquare className="text-white" size={24} />
                        </div>
                        <div>
                          <h3 className="text-xl sm:text-2xl font-bold text-white group-hover:text-emerald-400 transition-colors">{interview.jobRole}</h3>
                          <div className="flex flex-wrap items-center gap-3 sm:gap-4 text-xs sm:text-sm text-gray-400 mt-1">
                            <div className="flex items-center gap-1.5">
                              <Calendar size={14} />
                              <span>{formatDate(interview.createdAt)}</span>
                            </div>
                            <div className="flex items-center gap-1.5 px-2 py-1 bg-green-500/20 rounded-lg">
                              <Target size={14} className="text-green-400" />
                              <span className="text-green-300 font-medium">{interview.questions?.length || 0} questions</span>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div className="text-center sm:text-right">
                      <div className={`inline-flex items-center justify-center w-24 h-24 rounded-2xl border-4 shadow-xl ${
                        interview.overallScore >= 70 ? 'text-green-400 bg-green-500/20 border-green-500/40 shadow-green-500/20' :
                        interview.overallScore >= 50 ? 'text-yellow-400 bg-yellow-500/20 border-yellow-500/40 shadow-yellow-500/20' : 
                        'text-red-400 bg-red-500/20 border-red-500/40 shadow-red-500/20'
                      }`}>
                        <div>
                          <div className="text-3xl font-extrabold">{interview.overallScore}</div>
                          <div className="text-[10px] font-medium opacity-75">SCORE</div>
                        </div>
                      </div>
                      <div className="text-xs text-gray-400 mt-2 font-medium">Overall Performance</div>
                    </div>
                  </div>

                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6">
                    {interview.strengths && interview.strengths.length > 0 && (
                      <div className="bg-gradient-to-br from-green-500/10 to-emerald-500/10 border border-green-500/30 rounded-xl p-5 hover:border-green-500/50 transition-all">
                        <h4 className="text-sm font-bold text-green-400 mb-3 flex items-center gap-2">
                          <CheckCircle size={18} />
                          Your Strengths
                        </h4>
                        <ul className="text-sm text-green-100 space-y-2.5">
                          {interview.strengths.slice(0, 3).map((s: string, i: number) => (
                            <li key={i} className="flex items-start gap-2 leading-relaxed">
                              <span className="text-green-400 mt-0.5 font-bold">•</span>
                              <span>{s}</span>
                            </li>
                          ))}
                        </ul>
                      </div>
                    )}

                    {interview.improvements && interview.improvements.length > 0 && (
                      <div className="bg-gradient-to-br from-yellow-500/10 to-orange-500/10 border border-yellow-500/30 rounded-xl p-5 hover:border-yellow-500/50 transition-all">
                        <h4 className="text-sm font-bold text-yellow-400 mb-3 flex items-center gap-2">
                          <TrendingUp size={18} />
                          Growth Areas
                        </h4>
                        <ul className="text-sm text-yellow-100 space-y-2.5">
                          {interview.improvements.slice(0, 3).map((imp: string, i: number) => (
                            <li key={i} className="flex items-start gap-2 leading-relaxed">
                              <span className="text-yellow-400 mt-0.5 font-bold">•</span>
                              <span>{imp}</span>
                            </li>
                          ))}
                        </ul>
                      </div>
                    )}
                  </div>
                </div>
              ))
            ) : (
              <div className="backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-12 sm:p-16 text-center">
                <MessageSquare className="text-gray-500 mx-auto mb-6 opacity-50" size={80} />
                <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">No Interview Sessions Yet</h3>
                <p className="text-gray-400 text-base sm:text-lg mb-6 max-w-md mx-auto">
                  Practice with AI-powered mock interviews to ace your real interviews
                </p>
                <button
                  onClick={() => window.location.href = '/interview'}
                  className="px-8 py-4 bg-gradient-to-r from-green-500 to-emerald-500 text-white rounded-xl font-bold hover:scale-105 transition-all shadow-lg hover:shadow-green-500/50"
                >
                  Start Mock Interview
                </button>
              </div>
            )}
          </div>
        )}

        {/* Skill Gap Reports */}
        {activeTab === 'skillgap' && (
          <div className="space-y-6">
            {filteredData && filteredData.length > 0 ? (
              filteredData.map((gap: any, index: number) => (
                <div 
                  key={gap._id} 
                  className="group backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-6 sm:p-8 hover:bg-white/10 hover:scale-[1.02] hover:shadow-2xl hover:shadow-purple-500/20 transition-all duration-300 animate-slideUp"
                  style={{ animationDelay: `${index * 0.1}s` }}
                >
                  <div className="flex flex-col sm:flex-row items-start justify-between gap-4 sm:gap-0 mb-6">
                    <div className="flex-1">
                      <div className="flex items-center gap-3 mb-3">
                        <div className="p-2.5 bg-gradient-to-br from-purple-500 to-pink-600 rounded-xl group-hover:scale-110 transition-transform shadow-lg">
                          <TrendingUp className="text-white" size={24} />
                        </div>
                        <div>
                          <h3 className="text-xl sm:text-2xl font-bold text-white group-hover:text-purple-400 transition-colors">
                            {gap.jobId?.title || 'Job Analysis'}
                          </h3>
                          {gap.jobId?.company && (
                            <p className="text-sm sm:text-base text-purple-300 font-medium">{gap.jobId.company}</p>
                          )}
                          <div className="flex items-center gap-2 text-xs sm:text-sm text-gray-400 mt-1">
                            <Calendar size={14} />
                            <span>{formatDate(gap.createdAt)}</span>
                          </div>
                        </div>
                      </div>
                    </div>
                    <div className="text-center sm:text-right">
                      <div className={`inline-flex items-center justify-center w-24 h-24 rounded-2xl border-4 shadow-xl ${
                        gap.matchPercentage >= 70 ? 'text-green-400 bg-green-500/20 border-green-500/40 shadow-green-500/20' :
                        gap.matchPercentage >= 50 ? 'text-yellow-400 bg-yellow-500/20 border-yellow-500/40 shadow-yellow-500/20' : 
                        'text-red-400 bg-red-500/20 border-red-500/40 shadow-red-500/20'
                      }`}>
                        <div>
                          <div className="text-3xl font-extrabold">{gap.matchPercentage}%</div>
                          <div className="text-[10px] font-medium opacity-75">MATCH</div>
                        </div>
                      </div>
                      <div className="text-xs text-gray-400 mt-2 font-medium">Job Compatibility</div>
                    </div>
                  </div>

                  <div className="grid grid-cols-1 md:grid-cols-2 gap-4 sm:gap-6">
                    <div className="bg-gradient-to-br from-red-500/10 to-pink-500/10 border border-red-500/30 rounded-xl p-5 hover:border-red-500/50 transition-all">
                      <h4 className="text-sm font-bold text-red-400 mb-4 flex items-center gap-2">
                        <AlertCircle size={18} />
                        Skills to Develop ({gap.missingSkills?.length || 0})
                      </h4>
                      <div className="flex flex-wrap gap-2">
                        {gap.missingSkills?.slice(0, 8).map((skill: string, i: number) => (
                          <span key={i} className="px-3 py-1.5 bg-red-500/20 border border-red-500/40 text-red-200 rounded-lg text-xs font-medium hover:bg-red-500/30 transition-all">
                            {skill}
                          </span>
                        ))}
                      </div>
                    </div>
                    
                    <div className="bg-gradient-to-br from-purple-500/10 to-indigo-500/10 border border-purple-500/30 rounded-xl p-5 hover:border-purple-500/50 transition-all">
                      <h4 className="text-sm font-bold text-purple-400 mb-4 flex items-center gap-2">
                        <Target size={18} />
                        Learning Roadmap
                      </h4>
                      <div className="space-y-3">
                        <div className="flex items-center gap-3">
                          <div className="w-12 h-12 bg-purple-500/20 rounded-xl flex items-center justify-center flex-shrink-0">
                            <span className="text-purple-300 font-bold text-lg">{gap.recommendations?.length || 0}</span>
                          </div>
                          <div>
                            <p className="text-sm text-purple-200 font-medium">Learning Paths</p>
                            <p className="text-xs text-gray-400">Personalized for you</p>
                          </div>
                        </div>
                        <button className="w-full px-4 py-2 bg-purple-500/20 hover:bg-purple-500/30 border border-purple-500/40 text-purple-200 rounded-lg text-sm font-semibold transition-all hover:scale-105">
                          View Full Roadmap
                        </button>
                      </div>
                    </div>
                  </div>
                </div>
              ))
            ) : (
              <div className="backdrop-blur-lg bg-white/5 border border-white/10 rounded-2xl p-12 sm:p-16 text-center">
                <TrendingUp className="text-gray-500 mx-auto mb-6 opacity-50" size={80} />
                <h3 className="text-xl sm:text-2xl font-bold text-white mb-3">No Skill Gap Analyses Yet</h3>
                <p className="text-gray-400 text-base sm:text-lg mb-6 max-w-md mx-auto">
                  Analyze your skills against job requirements and get personalized learning paths
                </p>
                <button
                  onClick={() => window.location.href = '/skill-gap'}
                  className="px-8 py-4 bg-gradient-to-r from-purple-500 to-pink-500 text-white rounded-xl font-bold hover:scale-105 transition-all shadow-lg hover:shadow-purple-500/50"
                >
                  Analyze Skills Now
                </button>
              </div>
            )}
          </div>
        )}
      </div>
    </div>
  );
};

export default History;
